import React from 'react';
import {
  StyleSheet,
  Text,
  View,
} from 'react-native';

function FixtureScreen(): JSX.Element {
  
  return(
    <View><Text>fixture</Text></View>
  );
}

const styles = StyleSheet.create({
  sectionContainer: {
    marginTop: 32,
    paddingHorizontal: 24,
  },
});

export default FixtureScreen;
